[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "npx prisma generate --schema=./schema.prisma && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
