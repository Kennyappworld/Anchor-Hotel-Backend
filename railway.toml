[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "npx prisma migrate deploy --schema=./schema.prisma && node prisma/seed.js && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
